[
  {
    "id": "sample-demo",
    "claim": "Try a filled sample strip without opening or changing your project.",
    "where": "landing first screen, README",
    "test": "npm run test:e2e -- --grep @claim:sample-demo --project=chromium"
  },
  {
    "id": "editor-workflow",
    "claim": "Create, edit, reload, and export a frame-accurate event strip.",
    "where": "workspace, README",
    "test": "npm run test:e2e -- --grep @claim:editor-workflow --project=chromium"
  },
  {
    "id": "fps-options",
    "claim": "Plans strips at 12, 15, 24, 25, 30, or 60 frames per second.",
    "where": "project settings, README",
    "test": "npm run test:e2e -- --grep @claim:fps-options --project=chromium"
  },
  {
    "id": "audio-preview",
    "claim": "Stores local audio, calculates its waveform, and starts it at the aligned frame during preview.",
    "where": "workspace, quick guide, README",
    "test": "npm run test:e2e -- --grep @claim:audio-preview --project=chromium"
  },
  {
    "id": "local-storage-only",
    "claim": "Project data and media stay on this device during planning and export.",
    "where": "landing facts, footer, README",
    "test": "npm run test:e2e -- --grep @claim:local-storage-only --project=chromium"
  },
  {
    "id": "runtime-privacy",
    "claim": "Normal planning and export use no analytics, cookies, remote fonts, or third-party runtime scripts.",
    "where": "footer, privacy, README",
    "test": "npm run test:e2e -- --grep @claim:runtime-privacy --project=chromium"
  },
  {
    "id": "offline-reload",
    "claim": "Installs as a PWA and reopens the editor offline after the first visit.",
    "where": "landing facts, privacy, README",
    "test": "npm run test:e2e -- --grep @claim:offline-reload --project=mobile"
  },
  {
    "id": "project-json-roundtrip",
    "claim": "Project JSON exports a complete backup; valid backups reopen and invalid files leave the current project unchanged.",
    "where": "export dialog, README",
    "test": "npm run test:e2e -- --grep @claim:project-json-roundtrip --project=chromium"
  },
  {
    "id": "adapter-json-v1",
    "claim": "Adapter JSON exports every event with stable schema version 1.",
    "where": "handoff section, export dialog, README",
    "test": "npm run test:e2e -- --grep @claim:adapter-json-v1 --project=chromium"
  },
  {
    "id": "csv-export",
    "claim": "CSV export contains one UTF-8 row per event.",
    "where": "handoff section, export dialog, README",
    "test": "npm run test:e2e -- --grep @claim:csv-export --project=chromium"
  },
  {
    "id": "cached-license-offline",
    "claim": "A cached valid Studio license keeps paid downloads available offline.",
    "where": "license panel, README",
    "test": "npm run test:e2e -- --grep @claim:cached-license-offline --project=chromium"
  },
  {
    "id": "license-lifecycle",
    "claim": "Studio license checks use a local verdict for at most one day, never cache verification responses, restore access on another device, and lock paid downloads when a license is refunded, expired, or revoked while the free planner stays available.",
    "where": "license panel, privacy, terms",
    "test": "npm run test:e2e -- --grep @claim:license-lifecycle --project=chromium"
  },
  {
    "id": "studio-outputs",
    "claim": "The Studio Pack adds Godot 4 and Unity 6 adapter source plus a printable handoff sheet.",
    "where": "Studio Pack section, terms, README",
    "test": "npm run test:e2e -- --grep @claim:studio-outputs --project=chromium"
  },
  {
    "id": "studio-checkout",
    "claim": "The $12 one-time Studio Pack uses Sociobot's hosted checkout; production pages enforce the declared security and immutable-asset cache policy.",
    "where": "landing facts, Studio Pack section, README response policy",
    "test": "npm run test:live-policy -- --claim @claim:studio-checkout"
  }
]
